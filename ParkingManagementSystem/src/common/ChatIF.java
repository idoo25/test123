package common;

/**
 * Chat interface - keeping original for compatibility
 */
public interface ChatIF {
    /**
     * Method that when overridden is used to display objects onto a UI.
     */
    public abstract void display(String message);
}